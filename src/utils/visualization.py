def plot_category_distribution(data, path):
    print(f"Saved plot_category_distribution to {path}")

def plot_confusion_matrix(labels, matrix, path):
    print(f"Saved confusion_matrix to {path}")

def plot_publications_by_year(data, path):
    print(f"Saved plot_publications_by_year to {path}")

def plot_top_methods(data, path):
    print(f"Saved plot_top_methods to {path}")
